<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
	<h5 class="my-0 mr-md-auto font-weight-normal">Welcome</h5>

	<nav class="my-2 my-md-0 mr-md-3">
    <% if user_signed_in? %>
      <%= link_to 'Customers', customers_path, class: "p-2 text-dark" %>
      <%= link_to 'Officers', officers_path, class: "p-2 text-dark" %>
      
      <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "p-2 text-dark" %>
    <% end %>
  </nav>
</div>